name: gse_downloader
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python>=3.10
  - requests>=2.28.0
  - pandas>=2.0.0
  - loguru>=0.7.0
  - typer>=0.9.0
  - rich>=13.0.0
  - tqdm>=4.65.0
  - httpx>=0.24.0
  - aiofiles>=23.0.0
  - jsonschema>=4.0.0
  # NCBI SRA tools (optional, for raw sequence data)
  # - sra-tools
  # - fasterq-dump
dev-dependencies:
  - pytest>=7.0.0
  - pytest-asyncio>=0.21.0
  - pytest-cov>=4.0.0
  - black>=23.0.0
  - ruff>=0.0.270
  - mypy>=1.0.0
